# AI 驱动构建成果描述

## 项目名称
NL2SQL 智能问数系统

## 项目背景
企业使用 FineReport + FineBI 做 BI 分析，但缺少自然语言问数能力。业务人员查数据需要找 IT 写 SQL，效率低、响应慢。使用 AI Agent 从零构建了一套 NL2SQL 系统。

## AI Agent 参与方式
全程使用 Claude Code（AI Agent）作为唯一开发者，从零完成整个系统的设计、编码、调试和优化。人类仅提供需求方向和业务知识，所有代码实现均由 AI Agent 完成。

## 具体成果

### 1. 完整的 NL2SQL 系统（AI 独立构建）
- **后端**: Python + FastAPI，含 SSE 流式输出
- **前端**: 单页 HTML + Tailwind CSS，支持多轮对话
- **核心引擎**: 两步式 NL2SQL（选表 → 生成 SQL → 执行 → 总结）
- **LLM 抽象层**: 支持 Claude / 通义千问 / DeepSeek 一键切换
- **安全机制**: 仅允许 SELECT、自动加 LIMIT、超时控制

### 2. 智能 Schema 过滤（AI 自主设计）
- 面对 296 张数据库表，AI 设计了两步过滤策略：
  - 第一步：表名 + 注释 + 业务词典 → LLM 选出 3-8 张相关表
  - 第二步：只加载选中表的完整 schema 生成 SQL
- Token 消耗从 ~50K 降至 ~3K，成本降低 94%

### 3. 业务知识注入（AI 自动发现并修正）
- AI 在测试中发现 `area` 字段存的是业务团队名，不是地理区域
- 自动修正业务词典，将区域查询改为正确的 `zone` 字段
- 新增 5 个 few-shot 示例覆盖常见查询模式

### 4. 企业数据资产整合（AI 自主探索）
- AI 通过逆向工程 BI 系统 REST API，发现并提取了 2155 个报表目录
- 从数据库发现 110 条看板 SQL 定义
- 自动分析出：4 个可自然语言替代、80 个建议保留原报表、10 个高频核心查询
- 将现有 SQL 作为 few-shot 示例注入 NL2SQL 引擎，提升生成准确率

### 5. 错误自修复机制（AI 自主实现）
- SQL 执行失败时，自动将错误信息 + schema 反馈给 LLM 重试
- 自动处理中文别名 ORDER BY 问题
- 自动处理 Decimal 类型 JSON 序列化问题

## 技术指标

| 指标 | 数据 |
|------|------|
| 开发耗时 | 约 4 小时（含探索和调试） |
| 代码行数 | ~1500 行（后端 + 前端） |
| 支持表数 | 296 张（自动读取 schema） |
| 已验证查询 | 回款、出货、产品销量、区域对比、出货趋势 |
| 接入的现有报表 | 755 个活跃报表 + 110 条看板 SQL |
| LLM 支持 | 3 家（Claude、通义千问、DeepSeek） |

## AI Agent 的独特价值

1. **全栈能力**: 一个 Agent 完成后端、前端、数据库、API 对接，无需多人协作
2. **自主探索**: 自动探索 BI 系统 API、逆向分析 JavaScript、发现数据库中的报表定义表
3. **自我修正**: 测试中发现的问题（字段含义错误、SQL 语法问题）均由 AI 自动修复
4. **知识积累**: 将探索到的业务知识（表结构、字段含义、查询模式）自动沉淀为系统配置

## 实际效果

用户输入自然语言问题，系统 5-10 秒内返回：
- 生成的 SQL（可折叠查看）
- 数据表格（最多 50 行）
- AI 总结分析（自然语言回答）
